//
//  PLFMembershipTests.swift
//  PLFMembershipTests
//
//  Created by Platfarm on 2023/09/27.
//

import XCTest
@testable import PLFMembership

final class AlChemyServiceManagerTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }
    
    func test_builUrlString() throws {
        /*
        // Given:
        let method: String = "GET" // Assuming .get is a valid value
        let chain: String = "polygon" // Replace with a valid value
        let network: String = "mumbai" // Replace with a valid value
        let api: String = .transaction // Replace with a valid value
        let requests: [String: String] = [:] // Empty for this example
        let pathComponents: [String] = ["path1", "path2"]
        let queryParameters: [URLQueryItem] = [URLQueryItem(name: "param1", value: "value1")]
        
        // Instantiate the class containing the function. Replace 'YourClass' with the actual class name.
        let manager = AlchemyServiceManager.shared
        
        // When:
        let result = yourClassInstance.builUrlString(method: method,
                                                     chain: chain,
                                                     network: network,
                                                     api: api,
                                                     requests: requests,
                                                     pathComponents: pathComponents,
                                                     queryParameters: queryParameters)
        
        // Then:
        // Replace this with the expected URL string based on the input values provided.
        let expectedURLString = "EXPECTED_URL_STRING_HERE"
        XCTAssertEqual(result, expectedURLString)
         */
    }


}
